const range = 'z'-'a';
let valChar = Math.floor(Math.random() * range);
valChar = a + valChar;
const char = //convert asci code to char
//create a random letter

const guess = window.addEventListener(keypress, function (e) {
    const guess = e.key;


    if (/*not a char*/) {
      this.alert("nope, ")
    } 
    else { //if it is a char 

      if ((guess !== letter)) {
        if (Array wrong include guess){

        }
        else{add guess to Array}

      }
    }
  });


// function checkChar(event) {
//   e.targe;
// }
